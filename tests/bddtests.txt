1) close pool have to terminate all connections
2) последовательные запросы под одним пользователем должны использовать одно соединение
3) если в пуле нет свободных соединений и общий лимит исчерпан, то нужно убить idle соединение из другого пула
4) ? чтение большой таблицы
5) check pool idle timeout


1) Проверить, что пул убъет самое старое простаивающее соединение
2) Проверить кол-во простаивающих соединений
3) проверить, что создает не больше соединений чем можно

//проверил, нельзя создавать пул с connectionLimit меньше или равно нулю

1) добавить что при переиспользовании возвращается канал по нужным логину и паролю